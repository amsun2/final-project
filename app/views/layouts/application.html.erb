<!DOCTYPE html>
<html>
  <head>
    <title>Bookshelf</title>
    <%= csrf_meta_tags %>

    <!-- Expand the number of characters we can use in the document beyond basic ASCII 🎉 -->
    <meta charset="utf-8">

    <!-- Make it responsive to small screens -->
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>

    <!-- Connect Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">

    <!-- Connect Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=PT+Serif&display=swap" rel="stylesheet">
   
    <style>
      body {
        padding: 50px;
        font-family: 'PT Serif', serif;
      }
    </style>

  </head>


  <body>
    <p><%= notice %></p>

    <% if session[:user_id] != nil %>
      <div class="text-center">
        <a class="btn btn-primary" href="/"> My Shelf </a>
        <a class="btn btn-outline-primary" href="/authors"> Browse by author </a>
        <a class="btn btn-outline-primary" href="/books"> Browse by book </a> 
        <a class="btn btn-outline-primary" href="/genre_lists"> Browse by genre </a> 
        <a class="btn btn-secondary" href="/edit_user_profile"> Update account </a>
        <a class="btn btn-secondary" href="/user_sign_out"> Sign out </a>
      </div>
    <% end %>

    <%= yield %>
  </body>
</html>
